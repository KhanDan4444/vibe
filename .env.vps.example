# Future VPS / production server — copy to .env ON THE SERVER only
# Keep your laptop .env on localhost for development

PORT=5000
NODE_ENV=production

DB_USER=vibe
DB_HOST=localhost
DB_DATABASE=gym_saas
DB_PASSWORD=change-me-strong-password
DB_PORT=5432

JWT_SECRET=change-me-to-a-long-random-string

ALLOWED_ORIGINS=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com
TRUST_PROXY=1

PUBLIC_GYM_SIGNUP_ENABLED=1

# SMTP_HOST=...
# AFRO_MESSAGE_TOKEN=...
